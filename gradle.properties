# Enables Gradle Isolated Projects. This is what triggered SCA-5248:
# with it on, Gradle forbids `--no-configuration-cache`, which Mend's mendDeps
# init script passes. Unpatched Mend -> BUILD FAILED; patched Mend -> resolves deps.
org.gradle.unsafe.isolated-projects=true
