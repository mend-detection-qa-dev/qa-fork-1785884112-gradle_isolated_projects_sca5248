{
  "description": "SCA-5248: Gradle project with Isolated Projects enabled (org.gradle.unsafe.isolated-projects=true). Unpatched Mend fails to disable the configuration cache and resolves 0 deps; patched Mend resolves the full Gradle tree.",
  "version": 1.1,
  "base_config": {
    "validate_scan_output": true,
    "scan_output": {
      "total_libs_scanned": { "gte": 1 }
    },

    "validate_dependency_tree": true,
    "dependency_tree": {
      "check_dependency_tree_detection_status": true,
      "expected_direct_dependencies_number": { "gte": 3 },
      "expected_total_dependencies_number": { "gte": 5 },
      "direct_dependencies_names_list": { "contains": ["guava", "commons-lang3"] },
      "validate_agent_versions": false
    },

    "validate_vulnerabilities_table": true,
    "vulnerabilities_table": {
      "check_vulnerabilities_table_structure": true,
      "expected_total_vulnerabilities_number": { "gte": 0 }
    },

    "validate_scanner_logs": false
  }
}
